<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.czzruan.rrpremios">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/rr_premios_lauch"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/rr_premios_lauch"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activity.EnviarEmailActivity"></activity>
        <activity android:name=".activity.PagamentoActivity" />
        <activity android:name=".activity.ResultadosActivity" />
        <activity android:name=".activity.SobreActivity" />
        <activity android:name=".activity.ComoFuncionaActivity" />
        <activity android:name=".activity.EscolherNumeroActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.CadastroActivity" />
        <activity android:name=".activity.InicioActivity" />
        <activity android:name=".activity.EntradaActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>